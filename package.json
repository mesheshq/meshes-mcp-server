{
  "name": "meshes-mcp-server",
  "version": "1.0.0",
  "description": "MCP server for the Meshes event routing platform — lets AI agents emit events, manage workspaces, connections, rules, and debug deliveries.",
  "type": "module",
  "main": "dist/index.js",
  "license": "UNLICENSED",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx --env-file=.env src/index.ts"
  },
  "keywords": [
    "mcp",
    "meshes",
    "event-routing",
    "integrations",
    "webhooks",
    "crm"
  ],
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "jose": "^6.0.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
